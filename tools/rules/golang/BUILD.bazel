load("@bazel_skylib//:bzl_library.bzl", "bzl_library")

bzl_library(
    name = "defs",
    srcs = ["defs.bzl"],
    visibility = ["//visibility:public"],
    deps = [
        "//tools:defs",
        "//tools/platforms:transition",
        "@bazel_lib//lib:expand_template",
        "@bazel_lib//lib:transitions",
        "@rules_go//go:def",
        "@rules_oci//oci:defs",
        "@tar.bzl//:tar",
    ],
)
